[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP server for FAQ search"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.12.0",
    "minsearch>=0.0.4",
    "requests>=2.32.5",
    "toyaikit>=0.0.4",
]
