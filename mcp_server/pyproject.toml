[project]
name = "mcp-server"
version = "0.1.0"
description = "MCP server for FAQ search"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=2.13.1",
    "minsearch>=0.0.7",
    "requests>=2.32.5",
    "toyaikit>=0.0.4",
]
